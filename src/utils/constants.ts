export const constance = "http://161.35.153.209:5430/api";
export const PUBLISHABLE_KEY = import.meta.env.VITE_CLERK_PUBLISHABLE_KEY;
export const YANDEX_CLIENT_ID = "8bfc9f99e3064271ace9c89eb975a2cb";
export const REDIRECT_URI = import.meta.env.VITE_REDIRECT_URI;
export const CLIENT_SECRET = import.meta.env.VITE_CLIENT_SECRET;
export const jsonPlaceholderUrl = "https://jsonplaceholder.typicode.com"

// if (!constance) {
//   throw new Error("Missing Publishable Key");
// }
// if (!PUBLISHABLE_KEY) {
//   throw new Error("Missing Yandex Publishable Key");
// }
// if (!YANDEX_CLIENT_ID) {
//   throw new Error("Missing Yandex client ID");
// }
// if (!REDIRECT_URI) {
//   throw new Error("Missing Yandex Redirected URI");
// }
// if (!CLIENT_SECRET) {
//   throw new Error("Missing Yandex client secret");
// }

